# REVIEW F1 – Audit stato esecuzione Fase 1 (MVP)

**Data review:** 2026-04-02  
**Revisore:** Agente revisione fasi  
**Ambito:** verifica coerenza tra documentazione (`docs/`) e stato reale nel codice (`src/`, `tests/`) per F1.

---

## 1) Fonti analizzate

- `docs/PRD_Fase_1_MVP_detailed-1.md`
- `docs/tasks/master.yaml`
- `agents/AGENT_GUIDE.md`
- `agents/phase_1_mvp/STATUS.md`
- `agents/phase_1_mvp/GAPS_RISKS.md`
- Moduli runtime F1/F3 coinvolti: normalizer, listener, trade engine, bot/handlers, dispatcher, main
- Test F1/E2E eseguiti localmente

---

## 2) Esito sintetico

La **Fase 1 risulta eseguita e testata**: i task F1 in `master.yaml` sono `done`, lo `STATUS.md` F1 è `DONE`, e la suite di test F1/E2E passa (15/15).

Tuttavia, la review evidenzia che nel file `GAPS_RISKS.md` sono presenti voci ancora marcate *Aperto* che oggi hanno due nature diverse:

1. **Gap realmente ancora aperti** (tecnici/funzionali), da portare in backlog operativo.
2. **Gap superati da evoluzione successiva del codice** (es. integrazione `ListenerManager` in `main.py`), da riallineare nello stato documentale.

---

## 3) Verifica stato esecuzione F1

### Coerenza documentale
- `docs/tasks/master.yaml`: F1-01..F1-13 tutti `done`.
- `agents/phase_1_mvp/STATUS.md`: fase marcata `✅ DONE`, con evidenza test.

### Coerenza tecnica (spot check codice)
- Pipeline F1 presente: listener -> normalizer -> engine -> template -> draft.
- `EventNormalizer` usa mapping semplificato (`reduceOnly` => `CLOSE`, altrimenti `OPEN`).
- `TradeEngine` mantiene stato in memoria (`_positions`).
- `BinanceListener` ha reconnect con backoff ma senza keepalive esplicito del listenKey.
- `main.py` oggi usa `ListenerManager` (evoluzione oltre limitazione MVP iniziale).

### Evidenza test
Comando eseguito:

```bash
PYTHONPATH=. pytest -q tests/unit/test_normalizer.py tests/unit/test_trade_engine.py tests/unit/test_templates.py tests/integration/test_exchange_to_engine.py tests/integration/test_telegram_flow.py tests/e2e/test_full_pipeline.py
```

Risultato: **15 passed**, 1 warning config (`asyncio_mode`).

---

## 4) Gap/problemi rilevati (stato aggiornato in review)

### A) Gap ancora aperti (con impatto)

1. **F1-05 – Distinzione OPEN vs ADD non implementata**  
   - Stato review: **Aperto**  
   - Impatto: possibile gestione non corretta del lifecycle posizione in caso di incremento.

2. **F1-04 – listenKey keepalive non esplicitato nel listener**  
   - Stato review: **Aperto**  
   - Impatto: rischio stop silente stream su sessioni lunghe.

3. **F1-06 – Stato posizioni solo in memoria**  
   - Stato review: **Aperto (Alta priorità)**  
   - Impatto: perdita stato su restart/crash.

4. **F1-08 – Validazione token bot non esplicita in `init_bot()`**  
   - Stato review: **Aperto**  
   - Impatto: failure a runtime con diagnosi poco chiara.

5. **F1-09 – FSM edit senza timeout**  
   - Stato review: **Aperto (Bassa priorità)**

6. **F1-10 – Dispatcher senza retry specifico su 429**  
   - Stato review: **Aperto**

### B) Gap da riallineare documentalmente

1. **F1-11 – “ListenerManager non integrato”**  
   - In `GAPS_RISKS.md` risulta “Accettato (limitazione F1)”.  
   - Nel codice attuale `main.py` usa `ListenerManager.start_all()`.  
   - Stato review: **Da marcare superato/post-F1 implementato** per evitare incoerenza tra docs e runtime.

2. **F1-05 – Misclassificazione CLOSE su BUY FILLED**  
   - Già marcato “Chiuso” e coerente con implementazione corrente.

---

## 5) Valutazione finale fase

- **Gate F1 (MVP): PASS** sul piano di completamento task e regressione test.
- **Rischio residuo produzione:** **Medio-Alto** finché non vengono chiusi almeno:
  - persistenza/ripristino stato posizioni,
  - robustezza stream Binance (keepalive),
  - gestione retry Telegram.

---

## 6) Azioni raccomandate

1. Aggiornare `agents/phase_1_mvp/GAPS_RISKS.md` con sezione “stato al 2026-04-02 post-F3” per allineamento storico.
2. Aprire task tecniche dedicate (F3/F4 hardening o F5 reliability) per i 6 gap ancora aperti.
3. Aggiungere check automatici di resilienza (test restart state + test 429 retry + test stream longevity).
4. Mantenere in `STATUS.md` una distinzione esplicita tra:
   - “fase completata”,
   - “debito tecnico residuo”.

