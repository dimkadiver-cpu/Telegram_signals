# REVIEW F2 – Audit documentazione e implementazione Fase 2 (Metric Engine)

**Data review:** 2026-04-02  
**Revisore:** Agente revisione fasi  
**Ambito:** verifica coerenza tra documentazione F2 (PRD, task, status, gap) e implementazione reale (`src/`, `tests/`).

---

## 1) Fonti analizzate

- `docs/PRD_Fase_2_Metriche_detailed-1.md`
- `docs/tasks/master.yaml`
- `agents/AGENT_GUIDE.md`
- `agents/phase_2_metrics/STATUS.md`
- `agents/phase_2_metrics/GAPS_RISKS.md`
- `src/metrics/calculator.py`
- `src/main.py`
- `src/templates/default/*.j2`
- `tests/unit/test_metrics.py`
- `tests/unit/test_templates.py`
- `tests/e2e/test_full_pipeline.py`

---

## 2) Esito sintetico

La **Fase 2 risulta funzionalmente completata** (task F2-01..F2-05 `done` e test dedicati passanti), ma la review rileva **3 aree di allineamento documentale/funzionale**:

1. un conflitto interno in `GAPS_RISKS.md` sul tema PnL di `sl_hit/tp_hit` (dichiarato non corretto, ma i template già renderizzano `realized_pnl`);
2. una regola PRD non rispettata in modo uniforme: in `add.j2` la leva può mostrare `None` invece di essere nascosta;
3. coverage test template non completa sui messaggi di chiusura specializzati (`sl_hit`/`tp_hit`) e sul comportamento di rendering in assenza di leva.

---

## 3) Verifica coerenza ID F2 e stato

- `master.yaml` riporta F2-01..F2-05 come `done`.
- `STATUS.md` F2 è coerente con stato `DONE` e con integrazione `MetricCalculator` nel pipeline (`main.py`).
- Le formule dichiarate nel PRD F2 sono coerenti con `MetricCalculator` (delta exposure, leva effettiva, risk %, risk USD, RR side-aware, edge case SL/TP mancanti).

Esito: **coerenza generale buona** tra task ID F2 e implementazione.

---

## 4) Gap/errori/conflitti rilevati

### A) Conflitto documentale in F2 GAPS

**Riferimento:** `agents/phase_2_metrics/GAPS_RISKS.md` (voce F2-04 su `sl_hit/tp_hit`).

- La voce indica che i template di chiusura “non mostrano PnL finale corretto”.
- Nel codice, `sl_hit.j2` e `tp_hit.j2` mostrano già `{{ realized_pnl }}`.

**Valutazione review:** conflitto tra documento gap e stato reale dei template.  
**Azione consigliata:** mantenere il gap come “coverage test incompleto” (se necessario), ma correggere l’impatto perché il PnL è già visualizzato.

### B) Regola PRD “nascondere metriche non disponibili” applicata in modo parziale

**Riferimento:** `docs/PRD_Fase_2_Metriche_detailed-1.md`.

- La regola richiede di non renderizzare metriche `None`.
- In `open.j2` questa regola è rispettata con check espliciti.
- In `add.j2`, la riga della leva non verifica `metrics.effective_leverage is not none`; quindi può apparire testo tipo `Nonex` quando `capital_usd` non è valorizzato.

**Valutazione review:** gap funzionale minore, ma reale rispetto al PRD.  
**Azione consigliata:** applicare in `add.j2` la stessa logica condizionale già usata in `open.j2`.

### C) Copertura test incompleta su scenari template F2

- `tests/unit/test_templates.py` copre bene `OPEN` e alcuni casi condizionali.
- Non risultano test mirati su rendering `ADD` quando `effective_leverage=None`.
- Non risultano test diretti su output `SL_HIT` e `TP_HIT`.

**Valutazione review:** non blocca la fase, ma lascia rischio regressioni su messaggi reali di chiusura e su output “None”.  
**Azione consigliata:** aggiungere test unitari template per i tre scenari sopra.

---

## 5) Controlli eseguiti in review

Comando eseguito:

```bash
PYTHONPATH=. pytest -q tests/unit/test_metrics.py tests/unit/test_templates.py tests/e2e/test_full_pipeline.py
```

Risultato: **10 passed**, 1 warning di configurazione pytest (`asyncio_mode`).

---

## 6) Valutazione finale fase

- **Completamento task F2:** PASS
- **Qualità implementativa F2:** buona
- **Allineamento documentale F2:** discreto, con conflitti minori da correggere
- **Rischio residuo:** basso-medio (principalmente di consistenza documentazione e rendering edge case)

---

## 7) Azioni raccomandate (priorità)

1. **P1 (doc):** aggiornare `agents/phase_2_metrics/GAPS_RISKS.md` correggendo la voce su `sl_hit/tp_hit` (PnL già presente).
2. **P1 (code):** correggere `add.j2` per non mostrare leva quando `effective_leverage=None`.
3. **P2 (test):** estendere `tests/unit/test_templates.py` con casi su `ADD` senza leva e su `SL_HIT/TP_HIT`.
4. **P3 (doc):** aggiungere in `STATUS.md` una mini-sezione “debito tecnico residuo F2” per distinguere “fase done” da “hardening aperto”.

---

## 8) Follow-up applicato (2026-04-02)

Le correzioni P1/P2/P3 richieste in review sono state applicate:

- `add.j2` ora nasconde `Leva` quando `metrics.effective_leverage` è `None`.
- `tests/unit/test_templates.py` esteso con test su `ADD` senza leva e rendering `SL_HIT`/`TP_HIT`.
- `GAPS_RISKS.md` aggiornato per chiudere il conflitto documentale su PnL e tracciare il fix template ADD.
- `STATUS.md` aggiornato con sezione esplicita di debito tecnico residuo F2.
